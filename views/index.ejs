<%- include header %>


<div class='col-md-8 col-md-offset-2'>
	<div class="form-signup-heading">POST</div>
	    <% blogs.forEach(function (blog, index) { %>
        <h4><strong><a href="/blog/<%= blog._id %>"><%= blog.title %></a></strong></h4>
        <div class="container">
        <div class="row">
        <% blog.images.forEach(function (image, index) { %>
        <div class="col-sm-2">   
        <img src="/uploads/<%=image%>" class="img-responsive" style="width:100%;height:150px" alt="Image">
        </div>
        <% }) %>
        </div>
        </div>
        <div class="content"><%- blog.content %> </div>

        <div class="row-fluid content-bottom">
            <div class="row">
                <div class="col-md-5">
                	<i class="fa fa-user"></i> <small> <%= blog.author_name %></small>
                    <br>
                    <i class="fa fa-clock-o"></i> </i><small><%= blog.create_at %></small>
                </div>
                <div class="col-md-2 pull-right">
                    <% if (user&&blog.author_name == user.name) {%>
                        <a href="/edit/<%=blog._id%>"><i class="fa fa-pencil fa-lg"></i></a>
                        <a href="/blog/<%=blog._id%>"><i class="fa fa-comments fa-lg"><small> <%= blog.comments.length %> </small></i></a>
                        <a href="/delete/<%=blog._id%>"><i class="fa fa-trash-o fa-lg"></i></i></a>
                    <% } else {%>
                        <a href="/blog/<%=blog._id%>"><i class="fa fa-comments fa-lg"><small> <%= blog.comments.length %> </small></i></a>
                    <% } %>
                </div>
            </div>
        </div>
    	<hr>
		<% }) %>
    </div>
</div>


<%- include footer %>